# web-scraper-pet

# Create virtual evironment

```bash
# create new virtual environement
python -m venv /path/to/new/virtual/environment

# activate virtual environment

```



# MongoDB server with Docker Compose

```bash
docker-compose up -d
```

export VIRTUAL_ENV_DISABLE_PROMPT=1

```bash
scrapy shell 'https://www.petstock.com.au/collections/dog?page=1&resultsPerPage=16&view=products'
scrapy shell 'https://www.petstock.com.au/products/ziwipeak-daily-dog-cuisine-chicken-wet-dog-food'
```